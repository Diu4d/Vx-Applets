<view bindtap="clickRow" data-id="{{item._id}}" data-idx="{{index}}" class="row" wx:for="{{dataList}}">
  <view class="title">{{index+1}}.标题:{{item.title}}</view>
  <view>阅读量:{{item.num}}</view>
</view>
